# 项目工作约定

## 事实来源与入口

这是局域网多人「北京地标盲猜」，不是线上账户产品。前端 React 18 + Vite，后端 FastAPI + WebSocket，高德 JS API 2.0 提供地图。以当前代码及测试为事实来源。

- `src/App.jsx`：固定玩家 `/`、管理员 `/admin`、大屏 `/screen` 路由；旧 `/r/CODE` 系列链接按角色转到固定入口。
- `src/GameRoom.jsx`：玩家与管理员共享流程；管理员复用同一连接切换房间管理/我的答题。
- `src/Panels.jsx`、`src/UI.jsx`：出题、名单、公共控件和指引；`src/Results.jsx`：成绩单、排名落印与最佳玩家展示。
- `src/arcade.css`：在基础 `styles.css` 后加载的城市挑战券视觉；`src/PixelArt.jsx` 提供本地原创城景和定位针角色；`src/Invite.jsx` / `src/invite.js` 用现有 `/api/lan` 和当前访问地址生成本地入场二维码，不生成 localhost 邀请码。
- `src/MapView.jsx`、`src/mapOverlays.js`、`src/loadAmap.js`：地图生命周期、答案叠加层、SDK 加载。
- `src/ws.js`：房间连接与操作确认；`src/session.js`：全局当前房间、换局通知及重连同步。服务端 state / 匹配 requestId 的 ack 才是成功依据。
- `backend/main.py`：有效 API/WS 入口；`backend/rooms.py`：房间状态、抽题、计分；`backend/landmarks.py`：系统题库与搜索兜底。
- `server/` 是未接入当前启动链路的历史 Node 实现；根 `main.py` 也不是业务入口。不要依据它们修改现行规则。

## 游戏契约

1. 阶段仅有 `lobby → playing → reveal`。每人贡献两个地点才就绪；至少一人就绪才能开始。
2. 房主可参赛，也可仅主持。开局时未就绪和中途加入者旁观本局，不补发题目。
3. 每位参赛者三题。仅一名就绪者时全部抽系统题；多人至少一题系统题，其余从他人贡献与系统池抽取。
4. 按去空白并忽略大小写的同名、或距离不超过 50 米判断同一地点。所有候选（包括补题）排除本人贡献，且本人的三道题不重复。系统不足时从内置目录补齐。
5. 地图固定展示天安门、鸟巢、北京西站、国贸四个公共 emoji 参照点（references），坐标源自服务器目录，属于明确允许公开的辅助信息。系统池、他人贡献及补题均排除同名或距参照点不超过 50 米的地点；贡献时同规则拒绝。参照针与游戏针分离，不参与结果自动取景。
6. 地图点击仅预览，显式确认才发送 guess。提交前可以改点、撤销；三题齐全才可提交，提交后锁定。
7. 使用经纬度球面直线距离，逐题取整到米再求和，总误差升序排名。客户端不能自行宣称得分。NaN、Infinity、越界坐标无效。
8. 全员提交自动揭晓，房主也可提前揭晓。未提交者即使已有部分钉点也不计分、不排名；缺失结果为 null，不能显示为零分。
9. 提交成功后仅本人立即获得自己的总误差、三题真点与误差。未提交者不能获得答案；揭晓前不向其他玩家、管理员或大屏公开个人成绩、真点与排行榜。大屏作答期间仅获得已提交玩家的猜测点；管理员作答期间不能获取大屏猜测集。全场揭晓后公开排名，管理员/大屏另收到所有人的三题明细。榜单使用服务端 playerId 与 rank，不由客户端自行排序生成名次。

## 身份与生命周期边界

全站只有一个当前房间，房间与成绩保存在单进程内存中，服务重启即丢失；客户端自动回到等待管理员建房。创建新房时清理创建超过 8 小时且不是当前房间的旧房，不能把这描述为严格实时到期。不要多 worker 运行当前后端。

全局 `/ws/session/{role}` 首帧及 sync 返回 `{type:"session",room:code|null}`。仅 admin 角色可以发送 create / restart，写请求携带 requestId；restart 还需原 code。服务端锁串行切换，重复请求不会多建房；旧房停止接受操作。管理路由和角色是可信局域网约定，不是身份鉴权。原 POST `/api/rooms` 返回 405。

同房间同角色昵称用于重连找回玩家，包括刷新恢复已确认进度；同名连接会接管旧连接，旧连接收到 4409 后不重试，页面提供更换昵称；并非账户认证。管理员 URL 也不是鉴权凭证。保持可信局域网使用定位，不擅自扩展账户、持久化或公网部署。

## UI 与交互约束

- 前端设计、改版、审阅与动效任务先读项目技能 [.agents/skills/landmark-ui-design/SKILL.md](.agents/skills/landmark-ui-design/SKILL.md)，再按问题加载其中的上游技能和参考。保留像素北京街机风格；上游通用字体、图标、圆角和配色建议服从本项目规则。
- 允许按需引入 Tailwind，使用 npm 本地依赖及构建，不使用浏览器 CDN。所有实际采用的 UI 字体、图片、图标、CSS、JS 和其他静态资源必须保存到项目并同源提供，包含其嵌套资源引用。运行时仅允许高德地图必要的 SDK、地图资源、瓦片与地点搜索联网；开发期可以查阅文档和下载依赖。不得热链参考站或保留外部占位资源。
- 中文、手机优先；浅天空蓝底、奶白面板、海军蓝正文、金黄主色与深薄荷绿；统一像素 SVG 与本地原创数字字体，具体 token 见 `src/styles.css` / `DESIGN.md`。
- 手机使用底部任务面板，桌面使用侧栏；当前题与主操作不藏进菜单。地图避让按实际面板尺寸计算。UI 的 padding 使用 CSS 顺序，高德 setFitView 的 avoid 必须转换为上、下、左、右，并留足标签宽度与高度。窗口变化时需等待 SDK 尺寸同步后重新取景；程序化地图跳转使用立即模式，避免旧动画跨阶段覆盖揭晓取景。禁用双击缩放，防止连续落针被解释为缩放；保留双指和滚轮缩放。
- 安全区、短屏、软键盘、长名称、滚动和触控区域需要验证；尊重 `prefers-reduced-motion`。
- 结算成绩从首帧使用服务端真实值；落印/路线/散点仅首次展示时短暂播放，不循环，不阻塞操作，同房刷新或切页签不重播。未提交或旁观者不播放成绩庆祝。
- 只有管理员能在 reveal 阶段“重开一局”，通过全局 restart 创建后继房间。所有玩家及大屏自动跟随，保留昵称/角色，清空贡献和答案；无需刷新或分享新链接。失败保留当前成绩，未确认请求不能自动重放。
- 玩家提交即展示个人成绩，最终名次等待全场揭晓；普通玩家没有建房或重开按钮。大屏结算每页六人，每十秒翻页，支持暂停/手动翻页，逐人展示三题地点及误差；地图同步当前页参与者，避免 30 人的全部标记挤在一起。
- 搜索要取消旧请求，区分加载/无结果/失败。断线时禁用操作；不排队重放命令、不抢先显示成功。
- `useRoom().send(message)` 返回 Promise；调用必须 await/catch。同一连接最多一个待确认操作，超时重新同步。
- 地图仅在真实 complete 后进入 ready；重试要清理失败脚本/实例。保留高德版权标识和无文字底图。
- 不新增协议字段或改公开接口，除非功能确有必要且更新对应测试。不要通过客户端隐藏替代服务端的数据隔离。

## 开发与验收

```sh
npm ci
uv sync
npm run dev:all
npm run build
npm run test:rules
npm run test:unit
npm run test:e2e
npm run test:recovery
```

默认前端 5173、后端 8000。前端代理 `/api` 和 `/ws` 到后端；端口覆盖方式见 README。测试使用本机 Chrome；实际游戏操作走 UI，高德地图不 mock。测试房间只影响本机内存。

`tests/test_rooms.py` / `tests/test_session.py` 验证规则、角色协议与数据边界；Node 测试验证房间/全局连接和加载恢复；`tests/game.spec.js` 验证手机与大屏真实流程及 30 人结算分页。`tests/load_session.py` 使用真实网络 WebSocket 验证 30 人并发。浏览器全套及压测必须各用独立、初始无当前房间的后端，不能混入正在玩的房间。截图和测试输出保存在忽略的 `artifacts/`。禁止保存含地图凭据 URL 的 trace/HAR 或日志。接口测试通过不等于真实地图可玩。

## 密钥与协作

高德配置只放忽略的 `.env`（参照 `.env.example`），不提交、不在日志和报告展示值。JS Key 为浏览器所需配置，其他配置也不要写进源码。

跨模块工作先检查 git status/分支/worktree，使用独立 `codex/` 分支和 worktree。不要覆盖其他会话的修改；集成前重新检查主工作区，只合入本次改动。只停止本任务创建的服务。修改规则、启动方式或 UI 流程后同步 README/PLAYTEST 和相关测试，验收失败或外部依赖缺失必须如实说明。
