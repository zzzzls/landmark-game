# 本机试玩与验收

## 启动与入口

```sh
npm ci
uv sync
npm run dev:all
```

保留现有 `.env`，不要打印或提交高德凭据。默认前端5173、后端8000；手机使用本机局域网IP的前端地址。

- `/`：玩家填写昵称，自动加入当前房间；没有房间时等待。
- `/admin`：管理员创建房间，切换房间管理/我的答题，开始、提前揭晓和重开。
- `/screen`：无需输入房间号，跟随管理员创建和重开。

管理员路由按可信局域网约定使用，不是账号鉴权。按 Ctrl+C 停止本次启动；房间在单进程内存中，重启后所有入口自动回到等待建房。多网卡机器需选择与手机互通的IP。

## 手动完整试玩

1. 两个独立手机会话打开玩家入口并填写不同昵称，大屏打开 `/screen`，确认均等待管理员。
2. 管理员打开 `/admin` 创建房间，确认全部页面自动加入。管理员可仅主持，也可在“我的答题”参赛。
3. 每人通过搜索贡献两个地点，预览再确认。天安门🚩、鸟巢🏟️、北京西站🚉、国贸🏙️为公共参照，不能贡献或被抽题；它们帮助辨认方向，不参与成绩取景。
4. 管理员开局。拖动、缩放真实无文字地图，点击预览并确认，完成三个地点。验证切题、修改和撤销，未完成时不能提交。
5. 玩家甲提交后立即显示自己的真实总误差、三题误差及连线，不能修改；玩家乙继续答题，不能获得甲的真点或成绩。大屏只展示允许公开的猜测点。
6. 全员提交后公布最终排名。大屏逐人显示三个地点、误差和总误差。提前揭晓时未提交者不计分、无名次；旁观者没有伪造题目。
7. 管理员点击“重开一局”，玩家和大屏自动换房，保留昵称、清空贡献与答案；普通玩家无重开入口。
8. 验证刷新恢复、离线期间禁用操作、错过重开后联网追上新房；同名同角色新连接接管旧页面，旧页面终止自动重连并可更换昵称。

30人结算每页最多六人，十秒轮播，支持暂停/继续及手动翻页；地图显示当前页参与者的连线。30名参赛者加1名仅主持人共六页，旁观者无题不计分。

## 自动复跑

浏览器测试共享唯一当前房间，必须串行执行；第一项要求后端没有当前房间。不要对正在玩的服务运行测试。

```sh
npm run build
npm run test:rules
npm run test:unit
# 另开终端启动独立、全新的服务
FRONTEND_PORT=5175 BACKEND_PORT=8002 npm run dev:all
# 保持上述服务运行，另一终端执行
PLAYTEST_BASE_URL=http://localhost:5175 npm run test:e2e
# 独立进程重启验收，自动启动/关闭自己的5176和18004
npm run test:recovery
```

并发测试使用另一个初始无房间的独立后端：

```sh
# 终端一
uv run uvicorn backend.main:app --host 0.0.0.0 --port 18003
# 终端二：也可以将127.0.0.1替换为本机局域网IP
uv run python tests/load_session.py --url ws://127.0.0.1:18003 --players 30
```

`test:recovery` 会检查独立端口是否空闲，只停止自己创建的子进程；若端口已被占用，退出并报错，不终止未知服务。

## 资源本地化检查

资源检查会通过 UI 创建测试房间，必须使用独立、初始无当前房间的后端。以下端口仅是示例，先确认未被占用；保留本项目 `.env`，不要打印凭据。

```sh
npm run build
# 终端一：独立后端；检查结束后停止本次进程
FRONTEND_PORT=5188 uv run uvicorn backend.main:app --host 127.0.0.1 --port 18088 --no-access-log
# 终端二：Vite preview 沿用 server.proxy，指向上述后端
BACKEND_PORT=18088 npm run preview -- --host 127.0.0.1 --port 5188 --strictPort
# 终端三
PLAYTEST_BASE_URL=http://127.0.0.1:5188 npm run test:resources
```

三个独立 Chrome context 禁用缓存，覆盖玩家、管理员和大屏的等待页及建房后大厅，检查本地数字字体、真实地图 complete、四个参照点和成功瓦片响应。允许同源 HTTP/WebSocket，以及高德必要的 `amap.com` / `autonavi.com` 子域；其余外部请求导致失败。报告只保存域名、类型、数量和检查状态至 `artifacts/resource-audit.json`，不记录完整 URL、查询串、密钥或响应体，不生成 trace/HAR。

同时扫描 `src/`、`index.html` 和 `dist/` 中的脚本、样式、图片、字体及嵌套引用。代码内 SVG/XML 命名空间、文档链接不等于资源请求；远程懒加载资源则不能因冷启动未触发而漏审。此检查只验证上述场景的资源边界，不是整局、30 人或离线地图验收。

### 2026-09-20 设计技能与资源检查记录

- 项目安装 5 个固定版本上游技能，并新增 `landmark-ui-design`。6 个技能全部通过 frontmatter 校验，本地引用完整，安装版本和本地 SKILL 哈希匹配锁记录。
- 对“优化手机答题面板”“改进结算反馈”进行了指引桌面推演：分别选择 redesign/组件交互参考、Transitions，保留像素主题、地图避让和主操作；结算不得采用上游的数字滚动/延迟显现建议，仍使用首帧真实成绩。此项是指引审阅，不是页面改版或独立 Agent 实现验证。
- `npm run build` 通过；源码与生产包中的 UI 静态资源已在本地，外部运行依赖只发现高德 SDK 和瓦片。React 错误文档链接及 SVG/XML 命名空间不产生资源请求，没有待下载的外部 UI 素材。
- 在独立后端 `18188`、生产预览 `5198` 上通过 `npm run test:resources`：三种角色使用禁用缓存的新 context，字体加载成功，真实地图 ready、四个参照点正常；玩家/管理员各收到 12 个成功瓦片响应，大屏收到 54 个。非高德外部请求、本地资源错误和浏览器异常均为 0。
- 实际外部域名为 `webapi.amap.com`、`restapi.amap.com`、`jsapi.amap.com`、`jsapi-service.amap.com`、`custyle.amap.com` 及 `webrd01` 至 `webrd04.is.autonavi.com`，属于高德地图加载链路；浏览器未直连其他资源来源。

证据为忽略目录中的 `artifacts/resource-audit.json`、`artifacts/skill-validation.json`、`artifacts/skill-scenarios.md`。本次未修改页面视觉、游戏逻辑或依赖版本；Tailwind 已允许按需引入，尚未安装。未重跑整局和 30 人测试，既有记录的验证边界不扩大。

## 2026-09-20 最终改版记录

验收版本：固定默认房间、亮色像素主题、四个公共地标emoji参照、个人即时成绩及管理员联动重开。

| 验证 | 已验证结果 |
| --- | --- |
| 生产构建 | `npm run build` 通过 |
| 规则与协议 | 22项通过；含角色限制、建房重开幂等、隐私隔离、计分、旧房拒绝、接管连接、参照排除 |
| 前端单元 | 16项通过；含房间操作确认、全局连接心跳、断线恢复、不重放不确定写入、地图加载恢复 |
| 真实地图浏览器 | 完整7项通过，约1.1分钟；实际搜索、地图拖动/缩放/钉点，非地图mock |
| 局域网浏览器 | 在192.168.1.42:5175的HTTP来源实际重开、自动加入、加载真实高德、显示四个参照、拒绝参照出题并成功搜索贡献普通地点；零浏览器异常 |
| 服务重启 | 真实停止并重启独立后端；玩家/大屏回等待，昵称保留，建房后自动加入，地图及四个参照针恢复；零浏览器异常 |
| 30人并发游戏 | 经本机局域网地址192.168.1.42访问独立后端；181个操作，零错误；P50 66.43ms、P95 77.53ms、最大90.97ms |
| 30并发搜索 | 所有请求成功且非空；P50 379.54ms、P95 420.31ms、最大422.96ms |
| 轮播专项 | 修正交互暂停后，真实等待验证暂停11秒、继续后10秒自然翻页、再次暂停11秒；专项1项通过，44秒 |
| 30人大屏 | 逐页核对30名参赛者全部三题名称及误差，包含仅主持人的六页布局；无横向溢出 |
| 响应式与恢复 | 360/390/430手机，1440/1920桌面，1920×1080大屏；短屏、长名称、减少动态效果、地图/搜索错误重试、刷新与离线错过换房 |

期间修复：大批连接同时关闭导致的ASGI接收状态竞争、同名连接反复互相接管、玩家已提交时错误展示“还没有成绩”，以及继续轮播被交互焦点覆盖的问题。浏览器首次指引与软键盘滚动测试采用实际可见状态等待，避免测试时序误判。

证据存于忽略的 `artifacts/`：`load-30-lan.json`、`search-30-lan.json`、`service-restart.json`、`lan-browser.json`，以及 `screenshots/` 下的首页、贡献/答题、私人成绩、最终成绩、30人六页榜单和重启恢复截图。截图与自动化结果共同作为证据，不以截图代替完整游玩。测试不保留含凭据URL的trace/HAR。

**验证边界：**30人测试为本机通过真实网络连接模拟的并发玩家；多角色手机流程使用Chrome移动模拟和真实高德地图。未用30台实体手机在现场Wi-Fi同时测试，不能据此保证任意路由器、网络带宽或高德外部服务条件。现场仍需保证手机可访问前端端口并能连接高德地图。

## 2026-09-21 手机紧凑答题面板

- 玩家与管理员“我的答题”改为三题进度、两行题名、固定操作位；普通玩家底板205px，短屏193px，管理员另加54px角色切换，底部安全区只计算一次。确认和提交共用同宽操作位，取消修改保留原答案。
- `npm run build`、22项规则测试、16项前端单元测试通过；独立服务重启检查通过，真实地图重新加载并恢复四个参照点。
- 独立后端18191 / 生产预览5191上完整8项真实地图E2E通过：玩家与管理员各覆盖360/390/430宽及390×568短屏，地图近底板落点/取消、切题、保存/修改/撤销、提交、即时私人成绩、揭晓、换房、断线恢复、桌面侧栏及30人结算分页均通过。
- 新增真实WebSocket转发专项：发送一次无效题目ID，由真实后端拒绝；预览保留、进度不增加。暂扣真实确认回复期间，确认/取消/切题/管理员切换禁用，地图不可落点；释放回复后更新进度并自动下一题。没有mock高德或游戏规则。
- 长题名用DOM文字替换做隔离布局压力检查，安全区以CSS变量24px注入检查仅增加24px；测试后均恢复。另验证题名焦点、减少动态效果和1440桌面布局。此项不是实体手机刘海、软键盘或辅助技术的人工验收。
- 独立后端18192 / 生产预览5192的最终资源检查通过：三角色真实地图ready、四参照点、本地字体和成功瓦片响应正常；非允许外部请求、本地资源错误、浏览器异常均为0。资源检查覆盖等待页与大厅，完整答题由上述E2E独立覆盖。
- 截图复核统一取消/撤销宽度后，再复跑玩家全流程与管理员单人流程2项，并检查确认/提交按钮横向位置与宽度一致。

本轮证据保存在 `landmark-game-answer-panel/artifacts/`：`resource-audit.json`、`service-restart.json`，以及 `screenshots/player-answer-preview-390x844.png`、`player-answer-all-confirmed-390.png`、`admin-answer-preview-390x568.png` 等。Chrome移动模拟使用真实高德地图，未用实体手机或30台设备现场验收；未重跑独立30人并发压测，不扩展既有容量结论。

## 2026-09-21 城市挑战券视觉升级

- 入口改为北京像素城景、金黄招牌与入场券；成绩改为战绩票、真实总误差与名次落印；大屏加入本地二维码、玩家阵列和头名展示区。关卡游标与确认反馈不增加手机答题底板高度。
- 生产构建、22项规则测试、18项前端单元测试通过。新增邀请地址测试覆盖当前来源优先、localhost降级、虚拟网卡与非法地址过滤、重复地址及多网卡。
- 独立后端18201 / 生产预览5201的完整8项真实地图E2E通过；布局修正后复跑玩家全流程、单人管理员、30人分页3项。覆盖360/390/430手机、短屏、1440/1920桌面、真实贡献与落针、即时成绩与隐私、揭晓重开、断线恢复、服务端确认前禁用及拒绝后恢复。
- 30人结算新增“标题、头名、六行成绩和翻页控件在1920×1080同屏”的检查，防止自动化滚动掩盖溢出；全部六页逐人三题信息、暂停与自动翻页检查通过。少人数场景另经截图复核。
- 主工作区预览发现矮桌面窗口城景压到说明文字后，改用正常文档流排布插画，并增加1280×720断点和文字/城景不重叠检查；手机与桌面入口专项复跑通过。
- 最终独立后端18202 / 生产预览5202资源检查通过：三角色本地字体、真实地图ready及四参照点正常，大屏54个、玩家和管理员各12个成功瓦片响应；非允许外部请求、本地资源失败和浏览器异常均为0。二维码从实际大屏截图成功解码为本机局域网入口，没有使用外部二维码服务。
- 真实服务进程重启检查通过：玩家与大屏回到等待页、保留昵称，重新建房后自动加入并恢复地图及参照点。实际成绩始终从首帧显示；正常设置保留动效，仅在浏览器报告减少动态效果偏好时关闭动画。

证据保存在 `landmark-game-arcade/artifacts/`：`resource-audit.json`、`service-restart.json`、`qr-verification.json`，以及 `screenshots/home-390.png`、`home-1440.png`、`player-results-390.png`、`screen-lobby-1920.png`、`screen-reveal-1920.png`、`screen-30-players-page-1-1920.png`。未修改游戏规则或公开协议。

验证边界：手机为Chrome移动模拟与真实高德，30名玩家为真实WebSocket模拟，二维码为截图解码。未用实体手机扫描、未进行30台现场Wi-Fi设备测试，也未重跑独立并发压测；二维码正确不代表手机到主机网络必然互通。
