# 本机试玩与验收

## 启动和访问

在项目目录安装依赖并运行：

```sh
npm ci
uv sync
npm run dev:all
```

`.env` 须配置真实高德浏览器 Key、安全密钥；Web 服务 Key 用于地点搜索。不要覆盖已有 `.env`，也不要打印或提交其中的值。

- 首页：http://localhost:5173/
- 玩家：`/r/CODE`，输入昵称后加入。
- 房主：`/r/CODE/admin`，分为「房间管理」和「我的答题」。
- 大屏：`/r/CODE/screen`，建议 1920×1080 横屏。

首页建房后替换四位 CODE。手机与本机处于可互通网络时，使用房主页面提供的局域网邀请链接。不要把 localhost 链接直接发给其他设备。

默认前端 5173、后端 8000。端口冲突可使用 `FRONTEND_PORT=5174 BACKEND_PORT=8001 npm run dev:all`，无需改源码。终端按 Ctrl+C 停止本次启动的服务；不要停止其他任务的进程。房间在内存中，后端重启后需重新建房。

## 手动完整试玩

1. 手机窗口创建房间，关闭或看完三步指引。房主可保持「房间管理」只主持；要参赛则在「我的答题」贡献两个地点。
2. 用两个独立浏览器会话加入，分别搜索两个北京地点，点击搜索结果预览，再确认添加。两个槽位齐全后显示已准备。
3. 打开大屏。此时只展示加入人数、准备情况和房间号，不展示题目答案。
4. 房主开始游戏。未就绪者会被提示本局旁观；每位就绪玩家得到三个地名。多人不抽自己的地点，至少一题系统题；单人全部为系统题。
5. 手机拖动/缩放无文字地图，点击预览，再确认位置。切换三道题，可改点和撤销；三题齐全才可提交。
6. 第一人提交后等待，不能再改答案；大屏出现其猜测点，但没有真点、分数或排名。最后一人提交后自动揭晓。
7. 结果显示个人总误差、排名、逐题误差，地图显示猜测点与真点连线。可查看单题结果或收起面板看地图。未提交者明确显示未计分。
8. 提前揭晓需房主确认；未就绪和开局后加入者旁观。刷新会恢复同名同角色已确认的进度；断线期间操作禁用，恢复后重新同步。

手机界面使用底部任务面板，桌面使用侧栏；不再使用旧侧边抽屉。右上角可重看指引。搜索失败与空结果有不同提示；地图加载失败可重试。外部搜索不可用时服务端会回退内置北京目录。

## 自动验收

保持本地服务运行，执行：

```sh
npm run build
npm run test:rules
npm run test:unit
npm run test:e2e
# 使用其他前端端口时：
# PLAYTEST_BASE_URL=http://localhost:5174 npm run test:e2e
```

浏览器测试使用本机 Google Chrome 和独立上下文，无需账户。测试创建临时内存房间，会真实请求高德地图及搜索；不记录含凭据 URL 的 trace/HAR/video。

| 检查 | 入口 | 覆盖内容 |
| --- | --- | --- |
| 规则回归（14 项） | `tests/test_rooms.py` | 单人/多人抽题、排除本人同地点、系统题保证、未就绪无题、坐标合法性、撤销/提交限制、提前揭晓、总误差排序、揭晓前数据隔离 |
| 连接及地图加载（11 项） | `tests/*.test.mjs` | 服务端状态确认、重复操作、断线、终止错误、加载失败/超时/重试与清理 |
| 双玩家完整局 | `tests/game.spec.js` | 真实搜索、出题、刷新、触屏拖动/缩放、钉点、修改撤销、提交等待、自动揭晓、房主与大屏实时同步 |
| 单人房主参赛 | 同上 | 360px 手机、三道系统题、真实地图答题及结果 |
| 提前结束及重连 | 同上 | 晚加入旁观、实际 WebSocket 断开和网络恢复、已确认进度恢复、未提交不计分 |
| 入口和布局 | 同上 | 无效/不存在房间、三步指引、减少动态效果、360/390/430px 手机、1440/1920px 桌面和短屏 |
| 外部依赖恢复 | 同上 | 受控缺失地图配置→移除故障→真实地图成功；搜索请求失败/空结果/重试 |
| 同昵称不同角色 | 同上 | 房主与玩家同名参赛，各自显示正确成绩与排名 |

地图成功路径使用真实高德 SDK 和瓦片，不以占位地图代替。失败恢复场景才使用浏览器请求拦截制造缺配置、搜索失败和空结果；不注入游戏操作消息。测试监听服务端广播用于检查数据边界。

结果取景验证标记及文字位于可见地图内、避开实际面板，并在改变宽度后重新检查；截图等待地图布局稳定。自动保存关键截图到 `artifacts/screenshots/`，失败证据在 `artifacts/playwright-results/`，均不提交 Git。

## 实际验收记录（2026-09-20）

本次最终构建通过，14 项规则回归、11 项连接/加载测试、6 项浏览器场景全部通过；浏览器整组用时约 55 秒。真实高德地图已加载，双玩家完整局、单人房主参赛和大屏同步均完成，达到本机浏览器模拟环境下“可正常游玩”的标准。

验证中修复了揭晓取景的高德避让顺序、旧缩放动画覆盖新取景、窗口变化时 SDK 尺寸滞后、快速落针触发原生双击缩放覆盖揭晓，以及同昵称房主/玩家的成绩归属。截图包含手机准备、答题、等待、完整结果/单题结果及 1920×1080 大屏。

## 验收边界

本次为 Chromium 手机模拟和本机真实地图联网试玩；短屏覆盖可用视口缩小时的布局，不等于真实 iOS/Android 软键盘、刘海安全区或不同手机浏览器验收。尚未进行实体手机跨局域网联机测试。地图及搜索依赖网络和有效高德配置。

身份仍是同房间、同角色、同昵称恢复；不同设备使用相同身份会接管连接。当前没有管理员认证、持久化、多轮或公网部署功能。
